// depth
export const DEFAULT_DEPTH = 3
export const DEFAULT_MAX_DEPTH = 50
export const DEFAULT_MIN_DEPTH = 0
export const DEFAULT_APPROACHING_MIN_DEPTH = 1

// config
export const COLOR_PALETTE = 'dynamic'
export const RUIO_ENABLED = false
export const ROOT_ELEMENT_SELECTOR = '#root' // keep: used as fallback in auto-detection chain

// timing
export const ELEMENT_PICKER_DEBOUNCE_MS = 50
export const DROPDOWN_CLOSE_DELAY_MS = 350
export const CONFIG_RESET_DELAY_MS = 500
export const ICON_HOVER_TRANSITION_MS = 150
